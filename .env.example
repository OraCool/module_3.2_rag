# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Cohere API Configuration (for reranking)
# Get your API key from: https://dashboard.cohere.com/api-keys
# Free tier: 1,000 requests/month
COHERE_API_KEY=...

# Server Configuration
PORT=3000
NODE_ENV=development

# ChromaDB Configuration
# Run ChromaDB with Docker first:
# docker run -d -p 8000:8000 -v $(pwd)/data/chroma_db:/chroma/chroma --name chromadb chromadb/chroma:latest
CHROMA_DB_PATH=http://localhost:8000
CHROMA_COLLECTION_NAME=jmlr_papers

# RAG Configuration
# Number of candidates to retrieve in Stage 1 (vector search)
VECTOR_SEARCH_K=20
# Number of final results after Stage 2 (reranking)
RERANK_TOP_K=5
# Chunk size for document splitting (tokens)
CHUNK_SIZE=500
# Chunk overlap (tokens)
CHUNK_OVERLAP=50

# LLM Configuration
EMBEDDING_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini
MAX_TOKENS=2000
TEMPERATURE=0.7

# Logging
LOG_LEVEL=info
